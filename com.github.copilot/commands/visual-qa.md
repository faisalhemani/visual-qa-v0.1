---
description: Generate and run consented Visual QA for the web functionality just implemented
---

The user has explicitly requested Visual QA. Invoke the `visual-qa` skill now. Use the immediately preceding development request and implementation as the feature under test. Do not ask for a second confirmation unless running a command requires the host's normal command permission.
